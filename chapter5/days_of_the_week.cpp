#include "std_lib_facilities.h"

//Enter days of the week , followed by a number

int main(){
    cout<<"enter days of the week , followed by a number \n";
    string day;
    int num;
    while(cin >> day >> num){
    // this requires filling out functions for two hours,
    //every day of the week has a vector
    //i do if switches based on the day of the week
    //and push the int to that days vector
    vector<int>numbers;
    numbers.push_back(num);


    }
    }